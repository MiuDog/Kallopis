---
name: kallopis-design-contract
description: 在 Kallopis 或其消費端設計、實作、修正或審查 UI、布局、畫面、元件與互動體驗時使用。新布局先向使用者收集需求表單；改碼前確認體驗生命週期、三種繼承樹、語意定義、呈現所有權與測試定型階段，並維護唯一真相來源；不適用純資料、網路或無視覺影響的修改。
license: MIT
metadata:
  version: "0.2.0"
---

# Kallopis design contract

目標是讓視覺實作可追溯，而不是「看起來合理」。參考稿、Kallopis 語意與產品組裝是三種不同權限，不得互相代替。

## 必讀路由

每次觸發都先讀：

1. [風格繼承與實作前置檢查](references/style-preflight.md)。
2. [使用者布局契約](references/user-layout-contract.md)。
3. [設計知識唯一真相來源](references/design-knowledge/README.md)，再依其中路由讀取本次涉及的體驗、元件、畫面與語意文件。

若當次任務修改了新的視覺領域，再讀該領域的 `docs/architecture/components/` 文件、相關 token 類別與已接受的 decision；不要為了安心讀完整文件庫。

## 新布局需求閘門

當使用者提出新 screen、新區域、重大重排，或現有唯一真相來源尚未定義的布局時：

1. 第一個回應必須提供 [新布局需求規格表單](references/design-knowledge/layout-requirements-form.md)，請使用者填寫或逐欄確認。
2. 在必要欄位確認前，不得開始實作、不得從參考圖猜測缺少的布局、不得自行新增未被定義的元件或畫面。
3. 圖片與文字說明只能作為已填規格的佐證。若兩者衝突，列出衝突並請使用者決定權威來源。
4. 語意名稱、適用條件、風格結果與理由都必須明確確認；「看起來像」「一般慣例」或現有 token 名稱都不能代替定義。
5. 將確認內容寫入 `references/design-knowledge/` 對應文件後，才可進入屬性權限表與實作。

## 改碼閘門

任何會改變尺寸、間距、位置、對齊、圖層、顏色、字體、圓角、動態或互動狀態的修改，在第一個檔案變更前必須完成：

1. 從唯一真相來源取得本次體驗節點、screen、元件與語意；缺少的定義先與使用者確認並補寫。
2. 建立本次任務的「屬性權限表」：每項列出參考要求、目前實作來源、完整解析路徑、應修改的所有權層，以及是否仍有歧義。
3. 將屬性分成三類：
   - **精確幾何**：使用者或可量測設計稿指定的尺寸、距離、位置、對齊與圖層順序。不得取最接近的現有 token。
   - **Kallopis 語意**：使用者授權由設計系統決定的顏色、狀態或其他屬性。必須追到已解析 getter，不能只看到 `context.klp` 就假定語意正確。
   - **未確定**：來源無法量測、規則互相衝突或會導致兩種不同驗收結果。先向使用者確認，不得補值。
4. 在 commentary 用精簡文字回報已確認的體驗節點、三種繼承鏈、幾何權限、語意定義與未確定項。未完成不得實作。

## 三種繼承樹

每個被建立或修改的 screen 與複雜元件都必須在唯一真相來源維護：

1. **元件繼承架構**：複雜元件由哪些原生、Flutter 或 Kallopis 元件組成。
2. **畫面構成架構**：從 screen 到區域、容器、元件與內容的組合關係。
3. **風格／邏輯語意繼承樹**：語意如何解析成風格、排版、狀態或元件注入規則，以及採用該規則的理由。

關係圖一律使用 Mermaid；語意解析與注入條件一律另附程式碼區塊。只有自然語言敘述不算完成。

## 實作原則

- 參考稿要求精確幾何時，精確值仍應落在正確的 Kallopis semantic、geometry 或 component API；不要在產品端以 `EdgeInsets`、固定尺寸或局部 wrapper 建立平行規格。
- 現有 token 不符合參考稿時，修改或新增正確語意與測試；禁止以「語意較合理」為由改變已指定幾何。
- Kallopis 擁有可跨產品重用的呈現、元件狀態與組合規則；產品提供資料、狀態、事件與產品內容，只負責組裝。
- 元件只能讀 `KlpTheme` 已解析值。若 component token 可覆寫 semantic，先查 `KlpTheme` 對應 resolver，不能直接使用底層 primitive 或重做解析。
- 局部 surface 可能透過 `KlpTokenOverride` 改寫色彩；判讀實際顏色時必須沿 widget ancestor 檢查 override，不能只讀 app root theme。
- 變更圖層時，同時驗證 paint order、hit-test order 與 clip 邊界；只調 `Offset` 不算解決覆蓋問題。

## 從對話學習布局邏輯

把使用者的修正視為候選規則，但不要把單一畫面細節直接升級成全域偏好。

- 使用者明說「永遠」「之後都要」「Kallopis 應該」或同一規則跨任務重複出現時，將它加入 `references/user-layout-contract.md`。
- 每條規則記錄 scope、權限類型、來源與狀態。無法判定是單次例外或長期規則時，保留為本次任務條件並在有實質影響時詢問。
- 使用者的新指示可修正舊規則；保留修訂紀錄，不靜默改寫歷史意思。
- 不從使用者沉默、測試通過或 agent 自己的美感推導偏好。

## 驗收

視覺探索期不得撰寫或執行 UI test、golden test 或整合測試。靜態分析可用來發現語法與型別問題，但不能宣稱版面已定型。

- **單一元件定型**：使用者明確確認該元件後，才為該元件撰寫 component/widget test；測試不得擴張為尚未定型的總體版面契約。
- **總體版面未定型**：即使部分元件已有測試，也不得建立或更新全畫面 golden、screen integration 或端到端布局測試。
- **總體版面定型**：使用者明確確認完整 screen 與體驗流程後，才撰寫 golden test、screen integration 與必要的生命週期測試。
- 不得以既有測試期待值反推尚未討論的設計，也不得用更新 golden 掩蓋未解釋的差異。
- 進入對應定型階段後，才依 `AGENTS.md` 執行該階段允許的測試與驗證。
