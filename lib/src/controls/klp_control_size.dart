/// 控制項四段標準尺寸階級。
///
/// - `sm`: 32px 高度，搭配 14px (sub) 字級，12px 橫向內邊距。緊湊型按鈕、表格內 Tag、Search Bar。
/// - `md`: 40px 高度，搭配 16px (body) 字級，16px 橫向內邊距。預設按鈕、標準 Input 輸入框。
/// - `lg`: 48px 高度，搭配 16px/18px (lead) 字級，24px 橫向內邊距。主要 CTA 按鈕、手機端主要觸控區。
/// - `xl`: 56px 高度，搭配 18px (lead) 字級，32px 橫向內邊距。Hero 區塊的試用按鈕、全幅輸入框。
enum KlpControlSize { sm, md, lg, xl }
