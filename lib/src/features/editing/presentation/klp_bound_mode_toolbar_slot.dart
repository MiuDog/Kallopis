part of 'klp_editing_presentation.dart';

/// 驗證期 sentinel；renderer 不直接呈現此節點。
final class KlpBoundModeToolbarSlot extends KlpBoundTemplate {
	final String id;
	const KlpBoundModeToolbarSlot(this.id);
}
