# v1 最終證據稽核

50 個唯一 ID：9 個文件契約、41 個確定性／靜態證據，全部 acceptance 已映射。原獨立 48/50 稽核保留不覆寫；本次補 APP-V1-05／FEAT-V1-06 與 APP-V1-07 執行收據。

完整 CI 仍有已重現的原基準失敗，未宣稱發布 PASS；見[驗證與限制](verification.md)。完整可追查 JSON 在外部 `D:/Projects/Kallopis-v1-evidence/host-ports/v1-final-audit.json`，原工作樹回存收據另列精確最終 commit／hash。

| 切片 | acceptance | 證據 |
| --- | --- | --- |
| `APP-V1-01` | 已記錄公開匯出、目錄組裝、runtime 更新輸入及向上匯入的使用方。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `APP-V1-02` | 外部元件註冊不再能編譯；內建應用程式仍可建構與提交。 | External compiler rejects KlpApplication.components; legal built-in application declaration compiles, with router/session runtime integration also observed. |
| `APP-V1-03` | 28 個內建身分與實際展開順序相等；權責清冊及 session 整合檢查通過。 | 28 library-owned identities and adapter expansion/order are checked against the application ownership catalog; final CC catalog/closed/runtime group passed 38, later compatibility catalog/closure passed 55. |
| `APP-V1-04` | 匯入圖沒有 application 循環相依；在地化行為維持確定性。 | Actual declarative WidgetsApp installs the same lower localization delegate; Stable delegate override ordering and identity preserved. Final protected 179 covers host installation and full downward dependency. |
| `APP-V1-05` | Foundation 不再掌管宿主能力值；宣告式功能不直接執行檔案選取副作用。 | E pure resolver and real host 53 PASS; P action/legacy/public/catalog 105 PASS plus independent concurrent-action final file 13 PASS; frozen guards, scopes, source analysis and atlas pass. |
| `APP-V1-06` | 架構閘門回報 application 沒有不允許的跨模組 `internal` 匯入。 | 25 sources moved to named module contracts; 49 source/caller/metadata equivalence and 36 path substitutions preserved. Full application boundary 11, migration 170, catalogs 55 and architecture/token 167 passed. |
| `APP-V1-07` | 初始還原堆疊發布一次、導覽發布新堆疊、替換後的觀察者收到目前狀態，且拋出例外的觀察者可重試。 | Existing navigation restoration tests: 5 PASS; initial/navigation/replacement observer/retry evidence now executed. |
| `APP-V1-08` | 合法的內建組合可編譯，被禁止的公開名稱則不可編譯。 | Protected public barrel layer inventory and positive built-in declaration compile; negative cases reject action execution, template/semantic authorship, registry/validation/runtime and incomplete rail names. |
| `CAP-V1-01` | Capability 原始碼只匯入 kernel 與自身；公開匯出已分類為宣告式或 provider 契約。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `CAP-V1-02` | Provider 匯出入口與上層模組解析至明確契約路徑；沒有公開程式庫匯出 `internal/` 原始碼路徑。 | 42 editing exports plus 9 parts moved to named contracts within original provider 44 exports; no new submission/helper exports. Physical source/part identity and public provider tests passed, contract group 12 and existing public group 197. |
| `CAP-V1-03` | 新 BlockNote 功能不相依於已棄用的即時正文權威；舊版相容匯入仍可識別且範圍受限。 | Symbol/use-level compatibility classification retains provider/save/identity/shared geometry/handwriting contracts and all 44 exports. Five typed BlockNote use-boundary tests passed; no second body/layout/selection/undo authority introduced. |
| `CAP-V1-04` | Composition 可使用 L1 值選擇適應式結構；foundation 以相容轉送保留舊匯入，直到呼叫端完成遷移。 | Three pure enum declarations now have one capabilities/environment owner; old foundation exports preserve enum type identity/order. Five independent boundary/alias tests plus 28 behavior tests passed. |
| `COMP-V1-01` | 依原始碼證據列出公開與內部介面，以及目前的兩個循環。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `COMP-V1-02` | 建構邊界已交付；公開封閉、內建目錄與配對退役檢查通過。 | registry/catalog-contract.md fixes construction ownership; protected external compilation rejects registration/definition/registry inputs while legal library-owned construction remains; paired runtime/foundation retirement is exercised by CC 38 and direct-callers 150. |
| `COMP-V1-03` | composition 匯入不含 runtime/foundation 路徑；自適應行為維持確定性等價。 | Adaptive adapter/prepared implementation moved to runtime; composition retains structural nodes. Independent no-runtime/foundation boundary and 28 adaptive/catalog/runtime checks passed. |
| `COMP-V1-04` | composition 匯入不含 foundation 路徑；公開自適應輸入保留原有值與語意。 | Platform strategy consumes capabilities environment enums; forbidden composition→foundation edge rejected by protected boundary test, original enum values/type identity preserved. |
| `COMP-V1-05` | 沒有模組為了語意驗證而匯入另一模組的 `internal/` 路徑。 | Registry uses the named styling validation contract; module guard rejects cross-module resolution/internal access. Legal graphs, missing owner and cycles checked in protected semantic group 39. |
| `COMP-V1-06` | 公開介面清冊與負向編譯案例均與匯出入口一致。 | Protected external compilation and L0–L7 barrel scan reject definitions, validation, templates, semantics and runtime types while published nodes remain usable. |
| `FEAT-V1-01` | 已清點現行匯出、舊版匯出、外部相依、重複名稱及向上匯入。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `FEAT-V1-02` | 全部匯出／身分／資格／插槽與實際轉接器對照通過，錯誤清冊負向案例通過。 | features/catalog/component-ownership.json records 67 exports, 24 feature identities, 21 slots and two legacy name pairs; actual catalog cross-check and negative catalog fixtures are exercised by CC and later compatibility 55. |
| `FEAT-V1-03` | 已移除使用端轉接器附加；完整轉接器清冊已完成；15 個直接呼叫檔已改用具名 runtime 路徑；既有跨目錄相對匯入亦已修正。 | 15 feature caller files use named runtime paths; four pre-existing cross-directory relative imports repaired. Caller equivalence, 139 affected and 14 application/import-root passed. |
| `FEAT-V1-04` | Foundation 沒有功能／引擎匯入；功能行為與生命週期檢查通過。 | Editing/workspace prepared records and style now belong to features; 35 moved/caller implementations body-equivalent. Installation/session and ownership tests in protected 105 passed. |
| `FEAT-V1-05` | 功能匯入圖沒有 application 相依邊，且在地化行為維持確定性。 | 12 direct feature directives and indirect form-export consumers use foundation localization; full feature graph has no application edge. Final protected localization group 179 passed. |
| `FEAT-V1-06` | 宣告式功能契約不匯入 `file_selector`；使用端來源仍僅組合資料／事件。 | E pure resolver and real host 53 PASS; P action/legacy/public/catalog 105 PASS plus independent concurrent-action final file 13 PASS; frozen guards, scopes, source analysis and atlas pass. |
| `FEAT-V1-07` | 外部自訂子類別／定義不能進入有效樹；`KlpExplorer` 與 `KlpWindowControls` 各自只有一個現行正式來源。 | Seven public layout classes made final without other body/default/member changes. 55 external construction/subtyping/catalog checks pass, spoofed known identity rejected transactionally; P9 Stable legacy paths preserved. |
| `FEAT-V1-08` | 導覽可提交、被拒絕的根層返回不改變狀態，且已替換影格的回呼會撤銷；保留 `onPressed` 相容性，但不得與 `action` 同時提供。 | Actual router/session tests exercise semantic route actions, rejected navigation and stale callback revocation; source explicitly rejects action+onPressed together and preserves standalone onPressed. |
| `FEAT-V1-09` | `KlpRail`/`KlpRailItem` 不對使用端匯出，且工作區動作組合仍可編譯。 | External negative compiler cases reject KlpRail and KlpRailItem; positive workspace/application composition remains legal. |
| `FND-V1-01` | 匯入圖列出全部向上相依；公開匯出入口辨識有效與舊版介面。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `FND-V1-02` | 舊 definition/compiler/helper 已退役，公開封閉與保留行為檢查通過。 | Old component definition/compiler/definition-only template helper retired in CC; negative external compilation and unchanged library-owned runtime/caller behavior supported by CC 38 + 150. |
| `FND-V1-03` | foundation 匯入稽核不含 `features`、`application`、`rendering`、`runtime`、Krepis、Canva 或 BlockNote 匯入。 | All foundation directives, including barrel/part routes, exclude upper modules and Krepis/Canva/BlockNote. Generic prepared protocol retains 11 parts; protected presentation/boundary/renderer group 105 passed. |
| `FND-V1-04` | foundation 匯入稽核不含 `features` 目標；僅在必要位置透過匯出入口轉送，保留舊版公開相容性。 | Button style and selection toolbar ownership moved to features; Stable root forwarding preserves public name/type identity and action behavior. Protected stable-toolbar and body-equivalence evidence, 105 + 16 groups passed. |
| `FND-V1-05` | 沒有 styling 原始碼匯入 foundation；確定性的度量值保持不變。 | 14 metrics library/part files now belong to styling; foundation has one compatibility export. Protected 8 verifies 13 types, 150 values, single source and no styling→foundation edges; module DAG receipt shows no cycle. |
| `FND-V1-06` | 沒有 composition 原始碼匯入 foundation 平台值，且未引入第二份環境來源。 | Foundation retains only compatibility forwarding for the three capabilities-owned platform enums; five independent boundary/identity tests establish a single value source. |
| `FND-V1-07` | 沒有功能匯入 application，且不存在第二份 l10n 來源。 | Three localization files form one foundation-owned library; original 70 strings and optional seven BlockNote error strings retain identity/default/delegate behavior. Final 179 protected checks passed. |
| `FND-V1-08` | 合法公布節點可組合；外部 KlpTemplate／KlpTextSemantics 撰寫不可編譯。 | Table acceptance is unspecified ('原文件未另列。'); stated output is observable: external negative compiler cases reject KlpTemplate and KlpTextSemantics, while positive published-node composition compiles. |
| `KERN-V1-01` | Kernel 清冊顯示僅有 Dart SDK／內部匯入；宣告式匯出入口掌管三個公開契約；不存在上層匯入。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `REND-V1-01` | 已記錄匯入、渲染器分支、平台套件及向上的在地化相依邊。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `REND-V1-02` | Rendering 匯入圖沒有 application 相依邊，且在地化編輯行為維持確定性。 | Rendering uses foundation localization and no application edge; existing editing behavior and seven error branches keep lifecycle behavior. Protected 179 plus mounted-editor preservation 1 passed. |
| `REND-V1-03` | Foundation 沒有功能／引擎匯入；渲染器仍完整涵蓋封閉目錄。 | Renderer retains 26 dispatch types and six explicit nonvisual branches; real build rejects unknown prepared template. Protected prepared-renderer plus actual renderer/filter/catalog/import checks passed 105 and 16 groups. |
| `REND-V1-04` | 架構閘門回報沒有不允許的跨模組內部匯入。 | All renderer cross-module internal dependencies replaced by named lower contracts; body/directive identity preserved across 128 paths. Protected lower boundary 12 and migration/prepared 185 passed. |
| `REND-V1-05` | 確定性的生命週期／錯誤檢查證明每個工作階段只有一個所屬者與一條釋放路徑。 | R1 input/binding capture+idempotent detach and typed failure forwarding; R2 controller identity, fixed attachment, successful-bind ownership, stale-await cleanup and once-only open/error handling. R1 46, R2 38, regression 61, architecture 178 passed. Native Windows WebView and sensory quality remain human-pending. |
| `RUN-V1-01` | runtime 沒有公開匯出、不匯入上層 features/rendering/application 實作，且已辨識目前的自訂元件輸入。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `RUN-V1-02` | 沒有上層模組匯入 `runtime/**/internal`；runtime 行為檢查維持不變。 | 8 contracts and 3 named assembly/default placement entries physically moved; 11 old paths removed. Runtime body and caller equivalence receipts plus protected runtime-boundary tests: 139 affected + 14 application/import-root passed. |
| `RUN-V1-03` | 舊 components 輸入已移除；內建樹、未知身分拒絕與交易檢查通過。 | Runtime update/prepare context no longer accept components/compiler; catalog comes from library adapters. Unknown identity, tree transaction and caller checks are supported by CC 38 + 150. |
| `RUN-V1-04` | composition 沒有 runtime/foundation 匯入；自適應選擇與單一子節點具體化檢查通過。 | Runtime owns adaptive adapter and prepared resource behavior; 28 adaptive/catalog/runtime/scope checks and five boundary/enum checks passed. |
| `RUN-V1-05` | 沒有 runtime 檔案匯入另一模組的 `internal/` 解析器路徑。 | Runtime resolves through named styling contracts while keeping immutable resolution and styling authority; semantic group 39 and catalog/runtime/frame/import group 18 passed. |
| `STYLE-V1-01` | 匯入清冊證明現行路徑只相依於 kernel；公開匯出入口區分宣告式與舊版型別；反向 foundation 匯入已列為相容技術債。 | The module's PLAN READY contract contains the stated responsibility/import/public-surface inventory and required acceptance sections. Observed module-architecture checks validate exactly nine registered modules, matching first-level roots and complete contracts (3 checks); this documentation-only acceptance does not claim runtime or sensory validation. |
| `STYLE-V1-02` | 必要 styling 測試不使用像素基準；種類、完整性、循環與語意解析行為持續有確定性檢查。 | Direct root/example test scan found zero matchesGoldenFile/goldenFileComparator/screenMatchesGolden/multiScreenGolden/testGoldens references. Existing fixed primitive checks and semantic kind/completeness/cycle/resolver checks remain. Semantic protected group 39 supplies observed resolver evidence; a fresh primitive-only execution is not claimed or required for this retention-only slice. |
| `STYLE-V1-03` | Composition 無須匯入另一模組的 `internal/` 路徑即可驗證語意相依；使用端公開匯出維持不變。 | Named validateKlpSemanticGraph delegates to existing resolver construction without evaluation; resolver/resolution sources moved with body equivalence and no consumer export. Protected semantic/boundary group 39 passed. |
| `STYLE-V1-04` | 使用端可建構完整固定 primitive 集合，但不能建構樣式參照、語意鍵／token／schema 或第二份解析器輸入。 | External negative compiler cases reject KlpStyleRef/KlpSemanticKey/KlpSemanticToken/KlpSemanticSchema; legal KlpPrimitiveSet-based application remains constructible. Fixed primitive contract tests remain in source. |
