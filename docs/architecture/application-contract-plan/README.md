# APP-CONTRACT-V1-r1 application 具名契約收尾

APP-V1-06 complete。見 [驗證](verification.md) 與 [執行證據](execution.json)。

APP-CONTRACT-V1-r1：APP-V1-06 剩餘完整 application foreign-internal 邊以25個既有來源實體歸具名路徑收尾。Kernel三個lifecycle來源、capabilities導航machine/兩exception及五part、composition scope boundary、十一features adapters、rendering renderer/viewport入口依精確map搬移。Renderer沒有parts；其餘平台實作保留同模組internal，具名入口的正常implementation imports不是consumer公開或跨模組穿透。Navigation五part隨原owner同library移動，狀態機／transaction／commit／lease／錯誤／回收body與callback身分不變。不得以轉匯出barrel遮住其他模組internal，不留舊shim；所有root公開library export指令與順序保持，25來源仍不公開。所有directcallers與36個catalog metadata字串同步原ID/factory/variant/順序，只替換路徑。features67exports／24component、application28ID保持。四個原未附文件的adapter只新增精確用途dartdoc以維持token baseline45，其餘非directive正文僅准行首tab正規化；去除這四新增註解後正文等價。不在這批實作R1/R2 lifecycle、filepicker或環境；後續使用新的rendering具名入口。

[精確path-map](path-map.json)固定25moves、7module寫入邊界、36metadata值、所有tests與currentdocs。共同規格和各module先接受，然後每個乾淨base_revision發外置Task Packet並跑真實scope gate。Root公開barrels唯讀。文件owner更新現行參考與atlas briefs／source README，不改歷史plan/evidence snapshots。

M1：下層owner、原library／parts搬移；M2：application/runtime/callers與metadata閉包，正文／API等價；M3：獨立全applicationforeign-internal、25實體／private可達性、原navigationcommit/router/catalog／renderer／scope behavior與token gate通過，再atlas freshness與原樹回存。

每module cold-start 1k–4k tokens／5–25min，兩個作者各4k–10k／15–45min，假設同主模型、Windows Flutter/Node/Python正常，參考LOWER/COMPAT工作量但不假稱逐工時統計。越界立即停；超過12k tokens或60min回報有證據異常。新tests只因跨模組契約與navigation transaction風險，復用既有行為主體，不加感官或coverage要求。
